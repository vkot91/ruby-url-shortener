source "https://rubygems.org"

# Bundle edge Rails instead: gem "rails", github: "rails/rails", branch: "main"
gem "rails", "~> 8.0.5", ">= 8.0.5.1"
# Use postgresql as the database for Active Record
gem "pg", "~> 1.1"
# Use the Puma web server [https://github.com/puma/puma]
gem "puma", ">= 5.0"
# Build JSON APIs with ease [https://github.com/rails/jbuilder]
# gem "jbuilder"
# Use Redis adapter to run Action Cable in production
# gem "redis", ">= 4.0.1"

# Use Active Model has_secure_password [https://guides.rubyonrails.org/active_model_basics.html#securepassword]
# gem "bcrypt", "~> 3.1.7"

# Windows does not include zoneinfo files, so bundle the tzinfo-data gem
gem "tzinfo-data", platforms: %i[ windows jruby ]

# Reduces boot times through caching; required in config/boot.rb
gem "bootsnap", require: false

# Add HTTP asset caching/compression and X-Sendfile acceleration to Puma [https://github.com/basecamp/thruster/]
gem "thruster", require: false

# Use Active Storage variants [https://guides.rubyonrails.org/active_storage_overview.html#transforming-images]
# gem "image_processing", "~> 1.2"

# Use Rack CORS for handling Cross-Origin Resource Sharing (CORS), making cross-origin Ajax possible
# gem "rack-cors"

group :development, :test do
  # See https://guides.rubyonrails.org/debugging_rails_applications.html#debugging-with-the-debug-gem
  gem "debug", platforms: %i[ mri windows ], require: "debug/prelude"

  # Static analysis for security vulnerabilities [https://brakemanscanner.org/]
  gem "brakeman", require: false

  # Omakase Ruby styling [https://github.com/rails/rubocop-rails-omakase/]
  gem "rubocop-rails-omakase", require: false
end

# Application dependencies
gem "redis", "~> 5.3"
gem "connection_pool", "~> 2.4"
gem "sidekiq", "~> 7.3"
gem "bcrypt", "~> 3.1"
gem "jwt", "~> 2.9"

group :development do
  # Keeps the schema comment block at the head of each model, factory, and spec
  # in sync with the real table. Hooked into db:migrate via lib/tasks/annotate_rb.rake.
  gem "annotaterb", "~> 4.24", require: false
end

group :development, :test do
  # Loads backend/.env before Rails reads a single ENV.fetch. Nothing in this
  # app carries a fallback — an unset name raises at boot on purpose — so
  # running the server, a spec, or a console natively would otherwise mean
  # sourcing the file by hand every time. Development and test only: in
  # production the environment comes from the platform, and a .env file there
  # would be a second, quieter source of truth.
  gem "dotenv-rails"

  gem "rspec-rails", "~> 7.1"
  gem "factory_bot_rails", "~> 6.4"
  gem "database_cleaner-active_record", "~> 2.2"

  # Ruby LSP needs rubocop as a direct dependency to use it as the formatter.
  gem "rubocop", require: false
  gem "rubocop-rspec", require: false

  # Ruby LSP's test explorer only knows Minitest and Test::Unit natively; without
  # this addon it runs a spec file as `ruby -Ispec <file>`, which never loads
  # rspec-core and dies on `uninitialized constant RSpec`.
  gem "ruby-lsp-rspec", require: false
end
